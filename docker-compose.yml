services:
  traceability:
    build: .
    image: faceware_fvi
    container_name: faceware_fvi
    ports:
      - "5056:5056"
    restart: always
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
    networks:
      - manufacturing_network